# Havelaer

Personal homepage, built with a minimal static site generator.

## Development

```bash
pnpm install
pnpm dev      # build + watch
pnpm serve    # serve build/ locally
```

## Deploy

Pushes to `main` are automatically deployed to Scaleway Object Storage via GitHub Actions.
